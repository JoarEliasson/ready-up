# -----------------------------------------------------------------------------
# ReadyUp Bot Environment Configuration
#
# Copy this file to '.env' and fill in your values.
# The .env file should NOT be committed to version control.
# -----------------------------------------------------------------------------

# --- Required Settings -------------------------------------------------------

# Your bot's unique token from the Discord Developer Portal.
DISCORD_TOKEN=your_discord_bot_token

# --- Recommended Settings ----------------------------------------------------

# Discord server (guild) ID for instant slash-command registration.
# Leave blank to register commands globally (may take up to an hour).
GUILD_ID=your_discord_server_id

# --- Optional Settings -------------------------------------------------------

# Comma-separated role IDs with admin privileges for future commands.
# Example: ADMIN_ROLE_IDS=[123456789012345678,987654321098765432]
ADMIN_ROLE_IDS=

# Default timezone (IANA TZ database name).
DEFAULT_TIMEZONE=Europe/Stockholm

# Data directory path, relative to project root.
DATA_DIR=data

# Minutes after ETA before a user is considered a no-show.
ETA_EXPIRATION_MINUTES=60

# Hours of inactivity in an empty session before cleanup.
SESSION_INACTIVITY_TIMEOUT_HOURS=3
